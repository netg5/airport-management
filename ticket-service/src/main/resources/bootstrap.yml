server:
  port: 8086
  servlet:
    context-path: /ticket-api
spring:
  application:
    name: ticket-service
  cloud:
    config:
      uri: http://localhost:8079
      enabled: true
      discovery:
        enabled: true
        service-id: config-service