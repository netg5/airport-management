spring:
  cloud:
    config:
      name: ticket-service
      uri: http://localhost:8079
      enabled: true
      label: "master"
      discovery:
        enabled: true
        service-id: config-service