server:
  port: 8080
spring:
  application:
    name: zuul-gateway
  cloud:
    config:
      uri: http://localhost:8079
      enabled: true
      discovery:
        enabled: true
        service-id: config-service