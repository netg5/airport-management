spring:
  cloud:
    config:
      name: zuul-gateway
      uri: http://localhost:8079
      enabled: true
      label: "master"
      discovery:
        enabled: true
        service-id: config-service