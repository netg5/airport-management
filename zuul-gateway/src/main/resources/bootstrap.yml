spring:
  cloud:
    config:
      name: zuul-gateway
      uri: http://localhost:8888
      enabled: true
      label: "master" # branch where config is located
      discovery:
        enabled: true
        service-id: config-service