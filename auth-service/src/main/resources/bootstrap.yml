spring:
  cloud:
    config:
      name: auth-service
      uri: http://localhost:8079
      enabled: true
      label: "master" # branch where config is located
      discovery:
        enabled: true
        service-id: config-service
