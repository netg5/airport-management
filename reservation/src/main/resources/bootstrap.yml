spring:
  cloud:
    config:
      name: reservations
      uri: http://localhost:8888
      label: "master" # branch where config is located
      discovery:
        enabled: true
        service-id: config