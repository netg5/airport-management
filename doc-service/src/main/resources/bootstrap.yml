spring:
  cloud:
    config:
      name: doc-service
      uri: http://localhost:8079
      enabled: true
      label: "master"
      discovery:
        enabled: true
        service-id: config-service