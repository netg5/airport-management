server:
  port: 8085
  servlet:
    context-path: /docs
spring:
  application:
    name: doc-service
  cloud:
    config:
      uri: http://localhost:8079
      enabled: true
      discovery:
        enabled: true
        service-id: config-service