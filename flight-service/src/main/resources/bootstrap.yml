spring:
  cloud:
    config:
      name: flight-service
      uri: http://localhost:8079
      enabled: true
      discovery:
        enabled: true
        service-id: config-service
#eureka:
#  instance:
#    metadata-map:
#      management.context-path: /flight-api