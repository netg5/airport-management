spring:
  cloud:
    config:
      name: flight-service
      uri: https://localhost:8079
      enabled: true
      discovery:
        enabled: true
        service-id: config-service
    gateway:
      httpclient:
        ssl:
          useInsecureTrustManager: true