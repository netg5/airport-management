server:
  port: 8083
  servlet:
    context-path: /flight-api
spring:
  application:
    name: flight-service
  cloud:
    config:
      uri: http://localhost:8079
      enabled: true
      discovery:
        enabled: true
        service-id: config-service